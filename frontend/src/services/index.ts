// Barrel export file for services
export { truckerAPI } from './api';
export { default as api } from './api';
// Re-export types from utils
export type { TripRequest, TripResponse, TripStatus, HOSViolationType, Location, RouteSegment } from '../utils/types';
